[project]
name = "speachtt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.12.0",
    "datasets[audio]>=4.5.0",
    "torch>=2.10.0",
    "transformers>=5.0.0rc3",
]
